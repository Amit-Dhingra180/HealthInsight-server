def say_hello_logic(user_input):
    # Simple logic: If user says 'hello', respond with 'good morning'
    if user_input.lower() == 'hello':
        return 'Good morning'
    else:
        return 'I did not understand'
